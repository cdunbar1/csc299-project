[project]
name = "tasks4"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Carina Dunbar", email = "cdunbar1@depaul.edu" }
]
requires-python = ">=3.13"
dependencies = [
    "openai>=2.7.1",
]

[project.scripts]
tasks4 = "tasks4:main"

[build-system]
requires = ["uv_build>=0.9.7,<0.10.0"]
build-backend = "uv_build"
